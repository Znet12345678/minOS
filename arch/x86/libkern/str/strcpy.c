/*
*minOS libkern library
*Public domain
*strcpy function
*/
#include <libkern.h>
void strcpy(char *dest,const char *src){
	int i = strlen(dest);
	int i1 = 0;
	while(i < (strlen(dest) + strlen(src)){
		dest[i] = src[i1];
		i1++;
		i++;
	}
}
