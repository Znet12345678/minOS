#include <stdio.h>
#include <stdlib.h>
#include <string.h>
int calc(){
	kprintf("Enter number of terms:");
	char not[8];
	kstrcat(not,kgets());
	int noti = atoi(not);
	for(int i = 0; i < noti;i++){

	}
}
